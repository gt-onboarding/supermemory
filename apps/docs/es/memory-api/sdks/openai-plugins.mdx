---
title: "Plugins del SDK de OpenAI"
description: "Herramientas de memory para llamadas de funciones de OpenAI con integración de Supermemory"
---

Añade capacidades de memory a los SDKs oficiales de OpenAI usando las herramientas de llamadas de función de Supermemory. Estos plugins ofrecen una integración fluida con las completions de chat de OpenAI y las llamadas de función.

<CardGroup>
  <Card title="Supermemory tools on npm" icon="npm" href="https://www.npmjs.com/package/@supermemory/tools">
    Consulta la página de npm para más detalles
  </Card>

  <Card title="Supermemory AI SDK" icon="python" href="https://pypi.org/project/supermemory-openai-sdk/">
    Consulta la página de PyPI para más detalles
  </Card>
</CardGroup>

<div id="installation">
  ## Instalación
</div>

<CodeGroup>
  ```bash Python
  # Con uv (recomendado)
  uv add supermemory-openai-sdk

  # O con pip
  pip install supermemory-openai-sdk
  ```

  ```bash JavaScript/TypeScript
  npm install @supermemory/tools
  ```
</CodeGroup>

<div id="quick-start">
  ## Inicio rápido
</div>

<CodeGroup>
  ```python Python SDK
  import asyncio
  import openai
  from supermemory_openai import SupermemoryTools, execute_memory_tool_calls

  async def main():
      # Inicializar el cliente de OpenAI
      client = openai.AsyncOpenAI(api_key="your-openai-api-key")

      # Inicializar las herramientas de Supermemory
      tools = SupermemoryTools(
          api_key="your-supermemory-api-key",
          config={"project_id": "my-project"}
      )

      # Chatear con las herramientas de memory
      response = await client.chat.completions.create(
          model="gpt-5",
          messages=[
              {
                  "role": "system",
                  "content": "Eres un asistente útil con acceso a recuerdos de usuario."
              },
              {
                  "role": "user",
                  "content": "Recuerda que prefiero el té al café"
              }
          ],
          tools=tools.get_tool_definitions()
      )

      # Gestionar las llamadas a herramientas si existen
      if response.choices[0].message.tool_calls:
          tool_results = await execute_memory_tool_calls(
              api_key="your-supermemory-api-key",
              tool_calls=response.choices[0].message.tool_calls,
              config={"project_id": "my-project"}
          )
          print("Resultados de las herramientas:", tool_results)

      print(response.choices[0].message.content)

  asyncio.run(main())
  ```

  ```typescript JavaScript/TypeScript SDK
  import { supermemoryTools, getToolDefinitions, createToolCallExecutor } from "@supermemory/tools/openai"
  import OpenAI from "openai"

  const client = new OpenAI({
    apiKey: process.env.OPENAI_API_KEY!,
  })

  // Obtener las definiciones de herramientas para OpenAI
  const toolDefinitions = getToolDefinitions()

  // Crear el ejecutor de herramientas
  const executeToolCall = createToolCallExecutor(process.env.SUPERMEMORY_API_KEY!, {
    projectId: "your-project-id",
  })

  // Usar con OpenAI Chat Completions
  const completion = await client.chat.completions.create({
    model: "gpt-5",
    messages: [
      {
        role: "user",
        content: "¿Qué recuerdas sobre mis preferencias?",
      },
    ],
    tools: toolDefinitions,
  })

  // Ejecutar las llamadas a herramientas si las hay
  if (completion.choices[0]?.message.tool_calls) {
    for (const toolCall of completion.choices[0].message.tool_calls) {
      const result = await executeToolCall(toolCall)
      console.log(result)
    }
  }
  ```
</CodeGroup>

<div id="configuration">
  ## Configuración
</div>

<div id="memory-tools-configuration">
  ### Configuración de Memory Tools
</div>

<CodeGroup>
  ```python Python Configuration
  from supermemory_openai import SupermemoryTools

  tools = SupermemoryTools(
      api_key="your-supermemory-api-key",
      config={
          "project_id": "my-project",  # o utiliza container_tags
          "base_url": "https://custom-endpoint.com",  # opcional
      }
  )
  ```

  ```typescript JavaScript Configuration
  import { supermemoryTools } from "@supermemory/tools/openai"

  const tools = supermemoryTools(process.env.SUPERMEMORY_API_KEY!, {
    containerTags: ["your-user-id"],
    baseUrl: "https://custom-endpoint.com", // opcional
  })
  ```
</CodeGroup>

<div id="available-tools">
  ## Herramientas disponibles
</div>

<div id="search-memories">
  ### Buscar memories
</div>

Busca en las memories del usuario mediante búsqueda semántica:

<CodeGroup>
  ```python Python
  # Buscar memories
  result = await tools.search_memories(
      information_to_get="user preferences",
      limit=10,
      include_full_docs=True
  )
  print(f"Se encontraron {len(result.memories)} memories")
  ```

  ```typescript JavaScript
  // Buscar memories
  const searchResult = await tools.searchMemories({
    informationToGet: "user preferences",
    limit: 10,
  })
  console.log(`Se encontraron ${searchResult.memories.length} memories`)
  ```
</CodeGroup>

<div id="add-memory">
  ### Agregar memory
</div>

Almacena nueva información en memory:

<CodeGroup>
  ```python Python
  # Agregar memory
  result = await tools.add_memory(
      memory="El usuario prefiere el té al café"
  )
  print(f"Memory agregada con ID: {result.memory.id}")
  ```

  ```typescript JavaScript
  // Agregar memory
  const addResult = await tools.addMemory({
    memory: "El usuario prefiere café de tueste oscuro",
  })
  console.log(`Memory agregada con ID: ${addResult.memory.id}`)
  ```
</CodeGroup>

<div id="fetch-memory">
  ### Obtener memory
</div>

Recupera una memory específica por id:

<CodeGroup>
  ```python Python
  # Obtener una memory específica
  result = await tools.fetch_memory(
      memory_id="memory-id-here"
  )
  print(f"Contenido de la memory: {result.memory.content}")
  ```

  ```typescript JavaScript
  // Obtener una memory específica
  const fetchResult = await tools.fetchMemory({
    memoryId: "memory-id-here"
  })
  console.log(`Contenido de la memory: ${fetchResult.memory.content}`)
  ```
</CodeGroup>

<div id="individual-tools">
  ## Herramientas individuales
</div>

Usa las herramientas por separado para tener un control más granular:

<CodeGroup>
  ```python Python Individual Tools
  from supermemory_openai import (
      create_search_memories_tool,
      create_add_memory_tool,
      create_fetch_memory_tool
  )

  search_tool = create_search_memories_tool("your-api-key")
  add_tool = create_add_memory_tool("your-api-key")
  fetch_tool = create_fetch_memory_tool("your-api-key")

  # Usa herramientas individuales con la llamada de funciones de OpenAI
  tools_list = [search_tool, add_tool, fetch_tool]
  ```

  ```typescript JavaScript Individual Tools
  import {
    createSearchMemoriesTool,
    createAddMemoryTool,
    createFetchMemoryTool
  } from "@supermemory/tools/openai"

  const searchTool = createSearchMemoriesTool(process.env.SUPERMEMORY_API_KEY!)
  const addTool = createAddMemoryTool(process.env.SUPERMEMORY_API_KEY!)
  const fetchTool = createFetchMemoryTool(process.env.SUPERMEMORY_API_KEY!)

  // Usa herramientas individuales
  const toolDefinitions = [searchTool, addTool, fetchTool]
  ```
</CodeGroup>

<div id="complete-chat-example">
  ## Ejemplo completo de chat
</div>

Aquí tienes un ejemplo completo que muestra una conversación de varios turnos con memory:

<CodeGroup>
  ```python Complete Python Example
  import asyncio
  import openai
  from supermemory_openai import SupermemoryTools, execute_memory_tool_calls

  async def chat_with_memory():
      client = openai.AsyncOpenAI()
      tools = SupermemoryTools(
          api_key="your-supermemory-api-key",
          config={"project_id": "chat-example"}
      )

      messages = [
          {
              "role": "system",
              "content": """You are a helpful assistant with memory capabilities.
              When users share personal information, remember it using addMemory.
              When they ask questions, search your memories to provide personalized responses."""
          }
      ]

      while True:
          user_input = input("You: ")
          if user_input.lower() == 'quit':
              break

          messages.append({"role": "user", "content": user_input})

          # Obtener respuesta de la IA con herramientas
          response = await client.chat.completions.create(
              model="gpt-5",
              messages=messages,
              tools=tools.get_tool_definitions()
          )

          # Manejar llamadas a herramientas
          if response.choices[0].message.tool_calls:
              messages.append(response.choices[0].message)

              tool_results = await execute_memory_tool_calls(
                  api_key="your-supermemory-api-key",
                  tool_calls=response.choices[0].message.tool_calls,
                  config={"project_id": "chat-example"}
              )

              messages.extend(tool_results)

              # Obtener la respuesta final tras ejecutar las herramientas
              final_response = await client.chat.completions.create(
                  model="gpt-5",
                  messages=messages
              )

              assistant_message = final_response.choices[0].message.content
          else:
              assistant_message = response.choices[0].message.content
              messages.append({"role": "assistant", "content": assistant_message})

          print(f"Assistant: {assistant_message}")

  # Ejecutar el chat
  asyncio.run(chat_with_memory())
  ```

  ```typescript Complete JavaScript Example
  import OpenAI from "openai"
  import { getToolDefinitions, createToolCallExecutor } from "@supermemory/tools/openai"
  import readline from 'readline'

  const client = new OpenAI()
  const executeToolCall = createToolCallExecutor(process.env.SUPERMEMORY_API_KEY!, {
    projectId: "chat-example",
  })

  const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout,
  })

  async function chatWithMemory() {
    const messages: OpenAI.Chat.ChatCompletionMessageParam[] = [
      {
        role: "system",
        content: `You are a helpful assistant with memory capabilities.
        When users share personal information, remember it using addMemory.
        When they ask questions, search your memories to provide personalized responses.`
      }
    ]

    const askQuestion = () => {
      rl.question("You: ", async (userInput) => {
        if (userInput.toLowerCase() === 'quit') {
          rl.close()
          return
        }

        messages.push({ role: "user", content: userInput })

        // Obtener respuesta de la IA con herramientas
        const response = await client.chat.completions.create({
          model: "gpt-5",
          messages,
          tools: getToolDefinitions(),
        })

        const choice = response.choices[0]
        if (choice?.message.tool_calls) {
          messages.push(choice.message)

          // Ejecutar las llamadas a herramientas
          for (const toolCall of choice.message.tool_calls) {
            const result = await executeToolCall(toolCall)
            messages.push({
              role: "tool",
              tool_call_id: toolCall.id,
              content: JSON.stringify(result),
            })
          }

          // Obtener la respuesta final tras ejecutar las herramientas
          const finalResponse = await client.chat.completions.create({
            model: "gpt-5",
            messages,
          })

          const assistantMessage = finalResponse.choices[0]?.message.content || "No response"
          console.log(`Assistant: ${assistantMessage}`)
          messages.push({ role: "assistant", content: assistantMessage })
        } else {
          const assistantMessage = choice?.message.content || "No response"
          console.log(`Assistant: ${assistantMessage}`)
          messages.push({ role: "assistant", content: assistantMessage })
        }

        askQuestion()
      })
    }

    console.log("Chat con memory iniciado. Escribe 'quit' para salir.")
    askQuestion()
  }

  chatWithMemory()
  ```
</CodeGroup>

<div id="error-handling">
  ## Manejo de errores
</div>

Gestiona los errores de forma adecuada en tus aplicaciones:

<CodeGroup>
  ```python Python Error Handling
  from supermemory_openai import SupermemoryTools
  import openai

  async def safe_chat():
      try:
          client = openai.AsyncOpenAI()
          tools = SupermemoryTools(api_key="your-api-key")

          response = await client.chat.completions.create(
              model="gpt-5",
              messages=[{"role": "user", "content": "Hello"}],
              tools=tools.get_tool_definitions()
          )

      except openai.APIError as e:
          print(f"Error de la API de OpenAI: {e}")
      except Exception as e:
          print(f"Error inesperado: {e}")
  ```

  ```typescript JavaScript Error Handling
  import OpenAI from "openai"
  import { getToolDefinitions } from "@supermemory/tools/openai"

  async function safeChat() {
    try {
      const client = new OpenAI()

      const response = await client.chat.completions.create({
        model: "gpt-5",
        messages: [{ role: "user", content: "Hello" }],
        tools: getToolDefinitions(),
      })

    } catch (error) {
      if (error instanceof OpenAI.APIError) {
        console.error("Error de la API de OpenAI:", error.message)
      } else {
        console.error("Error inesperado:", error)
      }
    }
  }
  ```
</CodeGroup>

<div id="api-reference">
  ## Referencia de la API
</div>

<div id="python-sdk">
  ### SDK de Python
</div>

<div id="supermemorytools">
  #### `SupermemoryTools`
</div>

**Constructor**

```python
SupermemoryTools(
    api_key: str,
    config: Optional[SupermemoryToolsConfig] = None
)
```

**Métodos**

* `get_tool_definitions()` - Obtener definiciones de funciones de OpenAI
* `search_memories(information_to_get, limit, include_full_docs)` - Buscar memories del usuario
* `add_memory(memory)` - Agregar una nueva memory
* `fetch_memory(memory_id)` - Obtener una memory específica por id
* `execute_tool_call(tool_call)` - Ejecutar una llamada individual de herramienta

<div id="execute_memory_tool_calls">
  #### `execute_memory_tool_calls`
</div>

```python
execute_memory_tool_calls(
    api_key: str,
    tool_calls: List[ToolCall],
    config: Optional[SupermemoryToolsConfig] = None
) -> List[dict]
```

<div id="javascript-sdk">
  ### SDK de JavaScript
</div>

<div id="supermemorytools">
  #### `supermemoryTools`
</div>

```typescript
supermemoryTools(
  apiKey: string,
  config?: { projectId?: string; baseUrl?: string }
)
```

<div id="createtoolcallexecutor">
  #### `createToolCallExecutor`
</div>

```typescript
createToolCallExecutor(
  apiKey: string,
  config?: { projectId?: string; baseUrl?: string }
) -> (toolCall: OpenAI.Chat.ChatCompletionMessageToolCall) => Promise<any>
```

<div id="environment-variables">
  ## Variables de entorno
</div>

Establece estas variables de entorno:

```bash
SUPERMEMORY_API_KEY=your_supermemory_key
OPENAI_API_KEY=your_openai_key
SUPERMEMORY_BASE_URL=https://custom-endpoint.com  # opcional
```

<div id="development">
  ## Desarrollo
</div>

<div id="python-setup">
  ### Configuración de Python
</div>

```bash
# Instalar uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# Configurar proyecto
git clone <repository-url>
cd packages/openai-sdk-python
uv sync --dev

# Ejecutar pruebas
uv run pytest

# Verificación de tipos
uv run mypy src/supermemory_openai

# Formateo
uv run black src/ tests/
uv run isort src/ tests/
```

<div id="javascript-setup">
  ### Configuración de JavaScript
</div>

```bash
# Instalar dependencias
npm install

# Ejecutar pruebas
npm test

# Comprobación de tipos
npm run type-check

# Análisis de lint
npm run lint
```

<div id="next-steps">
  ## Próximos pasos
</div>

<CardGroup cols={2}>
  <Card title="AI SDK Integration" icon="triangle" href="/es/ai-sdk/overview">
    Úsalo con Vercel AI SDK para un desarrollo más ágil
  </Card>

  <Card title="Memory API" icon="database" href="/es/memory-api/overview">
    Acceso directo a la Memory API para una gestión avanzada de memories
  </Card>
</CardGroup>