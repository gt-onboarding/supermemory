---
title: "更新与删除记忆存储"
description: "通过 upsert 模式与幂等性，安全地更新和删除记忆存储"
icon: "delete"
---

可选择直接更新、幂等的 upsert、单项删除，以及强大的批量操作。

<div id="direct-updates">
  ## 直接更新
</div>

当你已明确要修改的特定 memory 时，可通过其 ID 更新现有项。变更会触发整条处理流水线的重新处理。

<CodeGroup>

```typescript Typescript
import Supermemory from 'supermemory';

const client = new Supermemory({
  apiKey: process.env.SUPERMEMORY_API_KEY!
});

// 通过 memory ID 更新
const updated = await client.memories.update('memory_id_123', {
  content: 'Updated content here',
  metadata: { version: 2, updated: true }
});

console.log(updated.status); // 重新处理时为 "queued"
console.log(updated.id); // "memory_id_123"
```

```python Python
from supermemory import Supermemory
import os

client = Supermemory(api_key=os.environ.get("SUPERMEMORY_API_KEY"))

# 通过 memory ID 更新
updated = client.memories.update(
    'memory_id_123',
    content='Updated content here',
    metadata={'version': 2, 'updated': True}
)

print(f"Status: {updated.status}")  # 重新处理时为 "queued"
print(f"ID: {updated.id}")  # "memory_id_123"
```

```bash cURL
curl -X PATCH "https://api.supermemory.ai/v3/documents/memory_id_123" \
  -H "Authorization: Bearer $SUPERMEMORY_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "content": "Updated content here",
    "metadata": {"version": 2, "updated": true}
  }'
```

</CodeGroup>

<div id="upserts-using-customid">
  ## 使用 customId 进行 Upsert
</div>

在需要幂等操作的场景下使用 `customId`：当以相同的 `customId` 调用 `add()` 时，会更新已有的 memory，而不是创建重复项。

<CodeGroup>

```typescript Typescript
import Supermemory from 'supermemory';

const client = new Supermemory({
  apiKey: process.env.SUPERMEMORY_API_KEY!
});

const customId = 'user-note-001';

// 首次调用：创建 memory
const created = await client.memories.add({
  content: 'Initial content',
  customId: customId,
  metadata: { version: 1 }
});

console.log('Created memory:', created.id);

// 使用相同 customId 的第二次调用：更新已有项
const updated = await client.memories.add({
  content: 'Updated content',
  customId: customId,         // 相同 customId = upsert
  metadata: { version: 2 }
});
```

```python Python
from supermemory import Supermemory
import os

client = Supermemory(api_key=os.environ.get("SUPERMEMORY_API_KEY"))

custom_id = 'user-note-001'

# 首次调用：创建 memory
created = client.memories.add(
    content='Initial content',
    custom_id=custom_id,
    metadata={'version': 1}
)

print(f'Created memory: {created.id}')

# 使用相同 customId 的第二次调用：更新已有项
updated = client.memories.add(
    content='Updated content',
    custom_id=custom_id,     # 相同 customId = upsert
    metadata={'version': 2}
)

print(f'Updated memory: {updated.id}')
print(f'Same memory? {created.id == updated.id}')  # True
```

```bash cURL
# 第一次调用 — 创建 memory
curl -X POST "https://api.supermemory.ai/v3/documents" \
  -H "Authorization: Bearer $SUPERMEMORY_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "content": "Initial content",
    "customId": "user-note-001",
    "metadata": {"version": 1}
  }'

# Response: {"id": "mem_abc123", "status": "queued", "customId": "user-note-001"}

# 第二次调用 — 使用相同 customId 更新已有项
curl -X POST "https://api.supermemory.ai/v3/documents" \
  -H "Authorization: Bearer $SUPERMEMORY_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "content": "Updated content",
    "customId": "user-note-001",
    "metadata": {"version": 2}
  }'

# Response: {"id": "mem_abc123", "status": "queued", "customId": "user-note-001"}
# Note: 返回相同 ID —— 表示进行了更新而非新建
```

</CodeGroup>

<Note>
`customId` 在所有端点均支持幂等性。`memoryId` 不支持幂等性，只有 `customId` 支持。
</Note>

<div id="single-delete">
  ## 单条删除
</div>

通过 ID 删除单个记忆存储。此操作为永久性硬删除，无法恢复。

<CodeGroup>

```typescript Typescript
// 硬删除 - 永久移除 memory
await client.memories.delete('memory_id_123');
console.log('Memory deleted successfully');
```

```python Python
# 硬删除 - 永久移除 memory
client.memories.delete('memory_id_123')
print('Memory deleted successfully')

# 单条删除的错误处理
try:
    client.memories.delete('memory_id_123')
    print('Delete successful')
except NotFoundError:
    print('Memory not found or already deleted')
except AuthenticationError:
    print('Authentication failed')
except Exception as e:
    print(f'Delete failed: {e}')
```

```bash cURL
curl -X DELETE "https://api.supermemory.ai/v3/documents/memory_id_123" \
  -H "Authorization: Bearer $SUPERMEMORY_API_KEY"

# 响应：204 No Content（成功）
# 响应：404 Not Found（memory 不存在）
```

</CodeGroup>

<div id="bulk-delete-by-ids">
  ## 通过 ID 批量删除
</div>

提供 memory 的 ID 数组即可一次性删除多个记忆存储。每个请求最多可包含 100 个 ID。

<CodeGroup>

```typescript Typescript
// 通过 memory ID 批量删除
const result = await client.memories.bulkDelete({
  ids: [
    'memory_id_1',
    'memory_id_2',
    'memory_id_3',
    'non_existent_id'  // 会在 errors 中报告
  ]
});

console.log('Bulk delete result:', result);
// 输出：{
//   success: true,
//   deletedCount: 3,
//   errors: [
//     { id: "non_existent_id", error: "Memory not found" }
//   ]
// }
```

```python Python
# 通过 memory ID 批量删除
result = client.memories.bulk_delete(
    ids=[
        'memory_id_1',
        'memory_id_2',
        'memory_id_3',
        'non_existent_id'  # 会在 errors 中报告
    ]
)

print(f'Bulk delete result: {result}')
# 输出：{
#   'success': True,
#   'deletedCount': 3,
#   'errors': [
#     {'id': 'non_existent_id', 'error': 'Memory not found'}
#   ]
# }
```

```bash cURL
curl -X DELETE "https://api.supermemory.ai/v3/documents/bulk" \
  -H "Authorization: Bearer $SUPERMEMORY_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "ids": [
      "memory_id_1",
      "memory_id_2",
      "memory_id_3",
      "non_existent_id"
    ]
  }'

# 响应：{
#   "success": true,
#   "deletedCount": 3,
#   "errors": [
#     {"id": "non_existent_id", "error": "Memory not found"}
#   ]
# }
```

</CodeGroup>

<div id="bulk-delete-by-container-tags">
  ## 按容器标签批量删除
</div>

删除指定容器标签中的所有记忆存储。适用于清理整个项目或用户数据。

<CodeGroup>

```typescript Typescript
// 在指定容器标签中删除所有记忆存储
const result = await client.memories.bulkDelete({
  containerTags: ['user-123', 'project-old', 'archived-content']
});

console.log('按标签批量删除结果：', result);
// 输出：{
//   success: true,
//   deletedCount: 45,
//   containerTags: ["user-123", "project-old", "archived-content"]
// }
```

```python Python
# 在指定容器标签中删除所有记忆存储
result = client.memories.bulk_delete(
    container_tags=['user-123', 'project-old', 'archived-content']
)

print(f'按标签批量删除结果：{result}')
# 输出：{
#   'success': True,
#   'deletedCount': 45,
#   'containerTags': ['user-123', 'project-old', 'archived-content']
# }
```

```bash cURL
curl -X DELETE "https://api.supermemory.ai/v3/documents/bulk" \
  -H "Authorization: Bearer $SUPERMEMORY_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "containerTags": ["user-123", "project-old", "archived-content"]
  }'

# 响应：{
#   "success": true,
#   "deletedCount": 45,
#   "containerTags": ["user-123", "project-old", "archived-content"]
# }
```

</CodeGroup>

<div id="advanced-patterns">
  ## 进阶范式
</div>

<div id="soft-delete-implementation">
  ### 软删除实现
</div>

对于需要审计追踪或恢复机制的应用，可通过 Metadata 实现软删除模式：

<CodeGroup>

```typescript Typescript
// 基于 Metadata 的软删除模式
await client.memories.update('memory_id', {
  metadata: {
    deleted: true,
    deletedAt: new Date().toISOString(),
    deletedBy: 'user_123'
  }
});

// 在搜索时排除已标记删除的记忆存储
const activeMemories = await client.memories.list({
  filters: JSON.stringify({
    AND: [
      { key: "deleted", value: "true", negate: true }
    ]
  })
});

console.log('Active memories:', activeMemories.results.length);
```

```python Python
from datetime import datetime
import json

# 基于 Metadata 的软删除模式
client.memories.update('memory_id', {
    'metadata': {
        'deleted': True,
        'deletedAt': datetime.now().isoformat(),
        'deletedBy': 'user_123'
    }
})

# 排除已标记删除的记忆存储
active_memories = client.memories.list(
    filters=json.dumps({
        "AND": [
            {"key": "deleted", "value": "true", "negate": True}
        ]
    })
)

print(f'Active memories: {len(active_memories.results)}')
```

```bash cURL
# 基于 Metadata 的软删除
curl -X PATCH "https://api.supermemory.ai/v3/documents/memory_id" \
  -H "Authorization: Bearer $SUPERMEMORY_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "metadata": {
      "deleted": true,
      "deletedAt": "2024-01-15T10:30:00Z",
      "deletedBy": "user_123"
    }
  }'

# Response: {"id": "memory_id", "status": "queued"}
```

</CodeGroup>

<div id="batch-processing-for-large-operations">
  ### 面向大型操作的批处理
</div>

<CodeGroup>

```typescript Typescript
// 安全地批量删除大量 memory
async function batchDeleteMemories(memoryIds: string[], batchSize = 100) {
  const results = [];

  for (let i = 0; i < memoryIds.length; i += batchSize) {
    const batch = memoryIds.slice(i, i + batchSize);

    console.log(`正在处理第 ${Math.floor(i/batchSize) + 1} 批（共 ${Math.ceil(memoryIds.length/batchSize)} 批）`);

    try {
      const result = await client.memories.bulkDelete({ ids: batch });
      results.push(result);

      // 批次间短暂停顿以避免触发速率限制
      if (i + batchSize < memoryIds.length) {
        await new Promise(resolve => setTimeout(resolve, 1000));
      }
    } catch (error) {
      console.error(`第 ${Math.floor(i/batchSize) + 1} 批失败：`, error);
      results.push({ success: false, error: error.message, batch });
    }
  }

  // 汇总结果
  const totalDeleted = results
    .filter(r => r.success)
    .reduce((sum, r) => sum + (r.deletedCount || 0), 0);

  console.log(`共删除：${totalDeleted} / ${memoryIds.length}`);
  return { totalDeleted, results };
}
```

```python Python
import time
import math

def batch_delete_memories(memory_ids, batch_size=100):
    """安全地批量删除大量 memory"""
    results = []

    for i in range(0, len(memory_ids), batch_size):
        batch = memory_ids[i:i + batch_size]
        batch_num = i // batch_size + 1
        total_batches = math.ceil(len(memory_ids) / batch_size)

        print(f'正在处理第 {batch_num} 批（共 {total_batches} 批）')

        try:
            result = client.memories.bulk_delete(ids=batch)
            results.append(result)

            # 批次间短暂停顿以避免触发速率限制
            if i + batch_size < len(memory_ids):
                time.sleep(1)
        except Exception as error:
            print(f'第 {batch_num} 批失败：{error}')
            results.append({'success': False, 'error': str(error), 'batch': batch})

    # 汇总结果
    total_deleted = sum(
        r.get('deletedCount', 0) for r in results if r.get('success')
    )

    print(f'共删除：{total_deleted} / {len(memory_ids)}')
    return {'totalDeleted': total_deleted, 'results': results}
```

```bash cURL
# 批处理脚本示例
#!/bin/bash

MEMORY_IDS=("id1" "id2" "id3")  # 你的 memory ID 数组
BATCH_SIZE=100
TOTAL_DELETED=0

# 按批处理
for ((i=0; i<${#MEMORY_IDS[@]}; i+=BATCH_SIZE)); do
    batch=("${MEMORY_IDS[@]:i:BATCH_SIZE}")
    batch_json=$(printf '%s\n' "${batch[@]}" | jq -R . | jq -s .)

    echo "正在处理第 $((i/BATCH_SIZE + 1)) 批"

    response=$(curl -s -X DELETE \
      "https://api.supermemory.ai/v3/documents/bulk" \
      -H "Authorization: Bearer $SUPERMEMORY_API_KEY" \
      -H "Content-Type: application/json" \
      -d "{\"ids\": $batch_json}")

    deleted_count=$(echo "$response" | jq -r '.deletedCount // 0')
    TOTAL_DELETED=$((TOTAL_DELETED + deleted_count))

    echo "本批删除：$deleted_count 条 memory"
    sleep 1  # 速率限制保护
done

echo "删除总计：$TOTAL_DELETED 条 memory"
```

</CodeGroup>

<div id="best-practices">
  ## 最佳实践
</div>

<div id="update-operations">
  ### 更新操作
</div>

1. **使用 customId 实现幂等更新** - 防止重复的 memory，并支持安全重试
2. **监控处理 status** - 更新会触发完整的重新处理流水线
3. **谨慎处理 Metadata** - 更新会替换指定的 Metadata 键
4. **完善错误处理** - 在两次操作之间，memory 可能被删除

<div id="delete-operations">
  ### 删除操作
</div>

1. **硬删除不可恢复** - 无任何回滚/恢复机制
2. **高效使用批量操作** - 每次批量删除最多包含 100 个 id
3. **考虑软删除方案** - 使用 Metadata 标记以实现可恢复删除
4. **分批执行大规模操作** - 通过合理分批避免触发速率限制
5. **清理应用状态** - 删除后更新 UI/缓存