---
title: "Infinite Chat"
description: "Contexte illimité pour les applications de chat avec gestion automatique des memory"
sidebarTitle: "Infinite Chat"
---

Infinite Chat offre un contexte illimité pour les applications de chat avec une gestion automatique des memory.

<div id="setup">
  ## Configuration
</div>

```typescript
import { streamText } from "ai"

const infiniteChat = createAnthropic({
  baseUrl: 'https://api.supermemory.ai/v3/https://api.anthropic.com/v1',
  apiKey: 'your-provider-api-key',
  headers: {
    'x-supermemory-api-key': 'supermemory-api-key',
    'x-sm-conversation-id': 'conversation-id'
  }
})

const result = await streamText({
  model: infiniteChat("claude-3-sonnet"),
  messages: [
    { role: "user", content: "Salut ! N'oublie pas que j'adore TypeScript." }
  ]
})
```

<div id="provider-configuration">
  ## Configuration du provider
</div>

<div id="named-providers">
  ### Fournisseurs nommés
</div>

<CodeGroup>
  ```typescript OpenAI
  const infiniteChat = createOpenAI({
    baseUrl: 'https://api.supermemory.ai/v3/https://api.openai.com/v1',
    apiKey: 'your-provider-api-key',
    headers: {
      'x-supermemory-api-key': 'supermemory-api-key',
      'x-sm-conversation-id': 'conversation-id'
    }
  })

  const result = await streamText({
    model: infiniteChat("gpt-5"),
    messages: [...]
  })
  ```

  ```typescript Anthropic
  const infiniteChat = createAnthropic({
    baseUrl: 'https://api.supermemory.ai/v3/https://api.anthropic.com/v1',
    apiKey: 'your-provider-api-key',
    headers: {
      'x-supermemory-api-key': 'supermemory-api-key',
      'x-sm-conversation-id': 'conversation-id'
    }
  })

  const result = await streamText({
    model: infiniteChat("claude-3-sonnet"),
    messages: [...]
  })
  ```

  ```typescript Google
  const infiniteChat = createGoogleGenerativeAI({
    baseUrl: 'https://api.supermemory.ai/v3/https://generativelanguage.googleapis.com/v1beta',
    apiKey: 'your-provider-api-key',
    headers: {
      'x-supermemory-api-key': 'supermemory-api-key',
      'x-sm-conversation-id': 'conversation-id'
    }
  })

  const result = await streamText({
    model: infiniteChat("gemini-pro"),
    messages: [...]
  })
  ```

  ```typescript Groq
  const infiniteChat = createGroq({
    baseUrl: 'https://api.supermemory.ai/v3/https://api.groq.com/v1',
    apiKey: 'your-provider-api-key',
    headers: {
      'x-supermemory-api-key': 'supermemory-api-key',
      'x-sm-conversation-id': 'conversation-id'
    }
  })

  const result = await streamText({
    model: infiniteChat("mixtral-8x7b"),
    messages: [...]
  })
  ```
</CodeGroup>

<div id="custom-provider-url">
  ### URL de provider personnalisé
</div>

```typescript
const infiniteChat = createOpenAI({
  baseUrl: 'https://api.supermemory.ai/v3/https://api.openai.com/v1',
  apiKey: 'your-provider-api-key',
  headers: {
    'x-supermemory-api-key': 'supermemory-api-key',
    'x-sm-conversation-id': 'conversation-id'
  }
})
```

<div id="example-usage">
  ## Exemple d’utilisation
</div>

```typescript
import { streamText } from "ai"

const infiniteChat = createOpenAI({
  baseUrl: 'https://api.supermemory.ai/v3/https://api.openai.com/v1',
  apiKey: 'your-provider-api-key',
  headers: {
    'x-supermemory-api-key': 'supermemory-api-key',
    'x-sm-conversation-id': 'conversation-id'
  }
})

const result = await streamText({
  model: infiniteChat("gpt-5"),
  messages: [
    { role: "user", content: "De quoi avons-nous parlé hier ?" }
  ]
})

return result.toAIStreamResponse()
```

<div id="configuration-options">
  ## Paramètres de configuration
</div>

```typescript
interface ConfigWithProviderName {
  providerName: 'openai' | 'anthropic' | 'openrouter' |
                'deepinfra' | 'groq' | 'google' | 'cloudflare'
  providerApiKey: string
  headers?: Record<string, string>
}

interface ConfigWithProviderUrl {
  providerUrl: string
  providerApiKey: string
  headers?: Record<string, string>
}
```

<div id="custom-headers">
  ### En-têtes personnalisés
</div>

Ajoutez des identifiants utilisateur, des identifiants de conversation ou d&#39;autres metadata :

```typescript
const infiniteChat = createOpenAI({
  baseUrl: 'https://api.supermemory.ai/v3/https://api.openai.com/v1',
  apiKey: 'your-provider-api-key',
  headers: {
    'x-supermemory-api-key': 'supermemory-api-key',
    'x-sm-conversation-id': 'conversation-id'
  }
})
```

<div id="comparison-with-memory-tools">
  ## Comparaison avec Memory Tools
</div>

| Fonctionnalité | Infinite Chat | Memory Tools |
|----------------|---------------|--------------|
| Gestion des memories | Automatique | Manuelle |
| Gestion du contexte | Automatique | Manuelle |
| Appels d’outils | Aucun | searchMemories, addMemory, fetchMemory |
| Idéal pour | Apps de chat | Agents IA |
| Complexité de configuration | Simple | Modérée |

<div id="headers">
  ## En-têtes
</div>

Ajoutez le contexte de l’utilisateur et de la conversation :

```typescript
const infiniteChat = createOpenAI({
  baseUrl: 'https://api.supermemory.ai/v3/https://api.openai.com/v1',
  apiKey: 'your-provider-api-key',
  headers: {
    'x-supermemory-api-key': 'supermemory-api-key',
    'x-sm-conversation-id': 'conversation-id'
  }
})
```

<div id="comparison">
  ## Comparaison
</div>

| Fonctionnalité | Infinite Chat | Memory Tools |
|---------|--------------|-------------|
| Gestion des memories | Automatique | Manuelle |
| Gestion du contexte | Automatique | Manuelle |
| Appels d’outils | Aucun | searchMemories, addMemory, fetchMemory |
| Idéal pour | Applications de chat | Agents IA |

<div id="next-steps">
  ## Prochaines étapes
</div>

<CardGroup cols={2}>
  <Card title="Memory Tools" icon="wrench" href="/fr/ai-sdk/memory-tools">
    Explorez le contrôle explicite des memories
  </Card>

  <Card title="Examples" icon="code" href="/fr/cookbook/ai-sdk-integration">
    Découvrez des mises en œuvre complètes
  </Card>
</CardGroup>